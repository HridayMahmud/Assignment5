# 🚌 Bus E-Ticketing System

A simple and interactive **Bus E-Ticketing Web Application** built using **HTML, Tailwind CSS, and JavaScript**.  
This system allows users to select bus seats with a strict limit and provides **coupons/rewards only when exactly 4 seats are selected**.
---
## 📌 Features

- 🎫 Interactive bus seat selection
- 🔢 Users can select **a maximum of 4 seats**
- ❌ Selection beyond 4 seats is not allowed
- 🎁 **Coupon/Reward is provided only if exactly 4 seats are selected**
- 🚫 No coupon if fewer than 4 seats are selected
- ⚡ Real-time validation and UI updates
- 📱 Fully responsive UI using Tailwind CSS

---

## 🛠️ Technologies Used

- **HTML5** – Structure of the application  
- **Tailwind CSS** – Utility-first responsive design  
- **JavaScript (Vanilla JS)** – Business logic and validations  

---

## ⚙️ How It Works

1. User selects bus seats.
2. The system allows selecting **up to 4 seats only**.
3. If the user tries to select more than 4 seats, the action is blocked.
4. **Coupon/offer is generated only when exactly 4 seats are selected.**
5. If the user selects fewer than 4 seats, **no coupon is applied.**

---

## 🚀 How to Run the Project

1. Clone this repository:
   ```bash
   git clone  https://github.com/HridayMahmud/Assignment5
